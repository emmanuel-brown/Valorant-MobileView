import React from 'react'
import './stripe.scss'

const StripeH = props =>{
    const { backgroundColor } = props
    return(
        <span style={{backgroundColor}} className="stripeH"></span>
    )
}

export default StripeH